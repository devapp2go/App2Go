import React from 'react';
import { connect } from 'react-redux';

class Instruction extends React.Component {
    render() {
      return (
        <div>
            <h2>Revisa las ordenes de tu App de Frutitas</h2>
        </div>
      );
    }
  }

  export default (Instruction);